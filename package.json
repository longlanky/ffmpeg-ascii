{
  "name": "ffmpeg-ascii",
  "version": "0.2.0",
  "description": "Takes an input video, converts it into ASCII frames, and prints them to screen",
  "main": "./main.js",
  "bin": {
    "ffmpeg-ascii": "./main.js"
  },
  "scripts": {
    "test": "node --test test/",
    "play": "node main.js play"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "Peter Zich",
  "dependencies": {
    "commander": "^14.0.0"
  }
}
